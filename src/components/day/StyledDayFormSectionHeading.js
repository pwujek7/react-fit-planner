import styled from 'styled-components';

import StyledText from '../common/styled/StyledText';

const StyledDayFormSectionHeading = styled(StyledText)`
  display: block;
  padding: 5px 0 5px 0;
  text-align: right;
`;

export default StyledDayFormSectionHeading;
